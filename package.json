{
  "name": "GameArchiver",
  "description": "",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "node ./app",
    "test": "env NODE_ENV=test mocha --recursive tests",
    "cover": "env NODE_ENV=test istanbul cover _mocha -- --recursive tests",
    "coveralls": "npm run cover -- --report lcovonly && cat ./coverage/lcov.info | coveralls"
  },
  "dependencies": {
    "@onehilltech/blueprint": "^1.5.1",
    "@onehilltech/blueprint-mongodb": "^0.14.0",
    "async": "^2.1.4",
    "chai": "^3.5.0",
    "codeclimate-test-reporter": "^0.4.0",
    "coveralls": "^2.11.15",
    "istanbul": "^0.4.5",
    "jsonwebtoken": "^7.1.9",
    "mocha": "^3.1.2",
    "mocha-lcov-reporter": "^1.2.0",
    "passport": "^0.3.2",
    "passport-http-bearer": "^1.0.1",
    "superagent": "^3.0.0",
    "supertest": "^2.0.1"
  },
  "devDependencies": {},
  "license": "Apache-2.0",
  "engines": {
    "node": ">=4.0.0",
    "npm": ">=3.0.0"
  }
}
